"use client";import{useState}from"react";import Link from"next/link";export default function Login(){const[show,setShow]=useState(false),[account,setAccount]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:account,password})}),d=await r.json();if(!r.ok)throw new Error(d.error||"登录失败");location.href=d.role==="admin"?"/admin":"/dashboard"}catch(e){setError(e instanceof Error?e.message:"登录失败")}finally{setLoading(false)}}return <main className="auth-page"><section className="auth-brand"><Link className="brand" href="/"><span>Y</span> YehaoProxy</Link><div><span className="kicker">WELCOME BACK</span><h1>让每一次全球连接<br/>都稳定可靠</h1><p>企业级代理网络，覆盖全球主要国家和地区。</p><ul><li>✓ 99.9% 网络可用率</li><li>✓ 7×24 小时技术支持</li><li>✓ 灵活 API 与批量管理</li></ul></div><small>© 2026 YehaoProxy</small></section><section className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><h2>登录账户</h2><p>欢迎回来，请输入你的账户信息</p>{error&&<div className="auth-error">{error}</div>}<label>邮箱或用户名<input type="text" value={account} onChange={e=>setAccount(e.target.value)} placeholder="邮箱或管理员用户名" required/></label><label><span>密码 <Link href="/forgot-password">忘记密码？</Link></span><div className="password"><input type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)}>{show?"隐藏":"显示"}</button></div></label><button className="primary auth-submit" disabled={loading}>{loading?"正在登录…":"登录"}</button><div className="auth-foot">还没有账户？ <Link href="/register">免费注册</Link></div></form></section></main>}
