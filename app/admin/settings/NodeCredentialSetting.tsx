"use client";
import {useEffect,useState} from "react";
export default function NodeCredentialSetting(){const[enabled,setEnabled]=useState(false),[saving,setSaving]=useState(false),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/settings").then(r=>r.json()).then(d=>setEnabled(d.options?.customer_node_credential_editing==="true")).catch(()=>setMessage("配置加载失败"))},[]);async function save(){setSaving(true);setMessage("");const r=await fetch("/api/admin/settings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind:"system-option",key:"customer_node_credential_editing",value:String(enabled)})}),d=await r.json();setSaving(false);setMessage(r.ok?"设置已保存，客户中心立即生效":d.error||"保存失败")}return <div className="setting-card node-credential-setting"><div className="setting-title"><div><h2>代理 IP 账号密码编辑</h2><p>控制客户是否可以自行修改已开通代理 IP 的认证账号和密码。</p></div><span>{enabled?"已开启":"已关闭"}</span></div><div className="setting-switch important"><div><b>允许客户编辑代理账号密码</b><small>关闭后客户中心保留入口并显示为灰色禁用状态，接口同时拒绝修改。</small></div><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/></div>{message&&<div className="settings-toast">{message}</div>}<footer><button type="button" className="primary" disabled={saving} onClick={save}>{saving?"保存中…":"保存功能设置"}</button></footer></div>}
