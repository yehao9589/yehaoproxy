"use client";import { useEffect,useState } from "react";type Log={id:string;actorId:string;actorRole:string;action:string;resourceType:string;resourceId:string|null;ipAddress:string|null;createdAt:string};export default function AuditClient(){const [items,setItems]=useState<Log[]>([]);useEffect(()=>{fetch("/api/admin/audit-logs?size=100").then(r=>r.json()).then(d=>setItems(d.items||[]))},[]);return <div className="standalone-admin"><header><a href="/admin">← 返回后台</a><h1>操作审计日志</h1></header><div className="standalone-table audit-table"><div className="logrow head"><span>时间</span><span>操作者</span><span>角色</span><span>操作</span><span>资源</span><span>IP 地址</span></div>{items.map(x=><div className="logrow" key={x.id}><span>{new Date(x.createdAt).toLocaleString()}</span><span className="mono">{x.actorId.slice(0,12)}…</span><span>{x.actorRole}</span><span>{x.action}</span><span>{x.resourceType} {x.resourceId?.slice(0,10)}</span><span>{x.ipAddress||"—"}</span></div>)}</div></div>}
