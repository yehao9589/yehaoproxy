"use client";
import {useState} from "react";
export default function CustomerCreateTool(){
 const[open,setOpen]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState("");
 async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setError("");const form=new FormData(event.currentTarget),body={name:String(form.get("name")||""),email:String(form.get("email")||""),password:String(form.get("password")||""),status:String(form.get("status")||"active"),emailVerified:form.get("emailVerified")==="on"};const response=await fetch("/api/admin/customers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),data=await response.json();setSaving(false);if(!response.ok){setError(data.error||"客户创建失败");return}setOpen(false);window.dispatchEvent(new CustomEvent("yehao:customer-created",{detail:{id:data.customer.id}}))}
 return <><div className="customer-create-entry"><div><small>客户账户</small><b>客户管理</b></div><button onClick={()=>{setError("");setOpen(true)}}>＋ 添加客户</button></div>{open&&<div className="customer-create-mask" onMouseDown={event=>{if(event.target===event.currentTarget&&!saving)setOpen(false)}}><form className="customer-create-modal" onSubmit={submit}><header><div><small>客户管理</small><h2>手动添加客户</h2><p>创建登录账户并自动初始化客户钱包。</p></div><button type="button" aria-label="关闭" onClick={()=>setOpen(false)}>×</button></header><div className="customer-create-fields"><label>客户名称<input name="name" maxLength={80} placeholder="请输入客户名称"/></label><label>登录邮箱<input name="email" type="email" required autoComplete="off" placeholder="name@example.com"/></label><label>初始密码<input name="password" type="password" minLength={8} required autoComplete="new-password" placeholder="至少 8 位，包含字母和数字"/></label><label>账户状态<select name="status" defaultValue="active"><option value="active">正常</option><option value="suspended">停用</option></select></label><label className="customer-create-check"><input name="emailVerified" type="checkbox"/><span><b>标记为邮箱已认证</b><small>仅在已确认邮箱真实有效时开启</small></span></label>{error&&<div className="customer-create-error">{error}</div>}</div><footer><button type="button" onClick={()=>setOpen(false)} disabled={saving}>取消</button><button className="primary" type="submit" disabled={saving}>{saving?"正在创建…":"创建客户"}</button></footer></form></div>}</>;
}
