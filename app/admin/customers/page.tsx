import { redirect } from "next/navigation";import { getCurrentCustomer } from "../../../lib/auth";import CustomersClient from "./CustomersClient";export const dynamic="force-dynamic";export default async function Page(){const u=await getCurrentCustomer();if(!u)redirect("/login");if(u.role!=="admin")redirect("/dashboard");return <CustomersClient/>}
