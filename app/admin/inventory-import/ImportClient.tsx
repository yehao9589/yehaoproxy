"use client";import{useState}from"react";export default function ImportClient(){const[message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMessage("正在校验并入库…");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/admin/inventory-bulk",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)return setMessage(d.error||"入库失败");setMessage(`成功入库 ${d.inserted} 条代理`);e.currentTarget.reset()}return <div className="standalone-admin"><header><a href="/admin">← 返回后台</a><h1>手动批量入库</h1></header><form className="module import-form" onSubmit={submit}><div className="form-grid"><label>产品类型<select name="product"><option value="static-isp">静态住宅 ISP</option><option value="residential">动态住宅</option><option value="datacenter">数据中心</option></select></label><label>国家/地区<input name="country" defaultValue="US" required/></label><label>城市<input name="city" placeholder="可选"/></label><label>协议<select name="protocol"><option>HTTPS</option><option>SOCKS5</option><option>HTTP</option></select></label><label>成本单价<input name="cost" type="number" min="0" step="0.01"/></label><label>销售单价<input name="salePrice" type="number" min="0.01" step="0.01" required/></label></div><label>代理数据<textarea name="lines" rows={15} required placeholder={"每行一条：host:port:username:password\n66.17.66.107:443:user001:password001"}/></label><div className="modal-note">密码将加密保存；系统会检查格式、本批次重复和库存重复。每次最多 5000 条。</div>{message&&<div className={message.startsWith("成功")?"auth-success":"live-error"}>{message}</div>}<button className="primary">校验并入库</button></form></div>}
