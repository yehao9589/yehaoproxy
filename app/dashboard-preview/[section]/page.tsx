import{redirect}from"next/navigation";const routes:Record<string,string>={static:"/dashboard/proxies",residential:"/dashboard/proxies",datacenter:"/dashboard/proxies",orders:"/dashboard/orders",billing:"/dashboard/wallet",api:"/dashboard/whitelist",support:"/dashboard/support"};export default async function Page({params}:{params:Promise<{section:string}>}){const{section}=await params;redirect(routes[section]||"/dashboard")}
