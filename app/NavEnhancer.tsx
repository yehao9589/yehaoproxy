"use client";import { useEffect } from "react";
export default function NavEnhancer(){useEffect(()=>{const paths=["/dashboard-preview","/dashboard-preview/static","/dashboard-preview/residential","/dashboard-preview/datacenter","/dashboard-preview/orders","/dashboard-preview/billing","/dashboard-preview/api","/dashboard-preview/support"];const handler=(e:Event)=>{const button=(e.target as HTMLElement).closest(".console-menu button") as HTMLButtonElement|null;if(!button)return;const buttons=Array.from(document.querySelectorAll(".console-menu button"));const index=buttons.indexOf(button);if(index>=0&&paths[index])location.href=paths[index]};document.addEventListener("click",handler);return()=>document.removeEventListener("click",handler)},[]);return null}
