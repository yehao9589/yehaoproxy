import { redirect } from "next/navigation";import { getCurrentCustomer } from "../../lib/auth";import LiveDashboard from "./LiveDashboard";export const dynamic="force-dynamic";export default async function Dashboard(){const user=await getCurrentCustomer();if(!user)redirect("/login");return <LiveDashboard name={user.name||user.email}/>}
