import { NextResponse } from "next/server";import { and,eq } from "drizzle-orm";import { getCurrentCustomer } from "../../../../lib/auth";import { getDb } from "../../../../db";import { orders,proxyAllocations } from "../../../../db/schema";import { audit } from "../../../../lib/audit";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentCustomer();if(!user)return NextResponse.json({error:"请先登录"},{status:401});const {id}=await params,b=await req.json().catch(()=>null),db=getDb(),[owned]=await db.select({allocation:proxyAllocations}).from(proxyAllocations).innerJoin(orders,eq(proxyAllocations.orderId,orders.id)).where(and(eq(proxyAllocations.id,id),eq(orders.customerEmail,user.email))).limit(1);if(!owned)return NextResponse.json({error:"代理不存在"},{status:404});const updates:{note?:string|null;autoRenew?:boolean}={};if(b.note!==undefined)updates.note=String(b.note).slice(0,200)||null;if(b.autoRenew!==undefined)updates.autoRenew=Boolean(b.autoRenew);if(!Object.keys(updates).length)return NextResponse.json({error:"没有可修改内容"},{status:400});await db.update(proxyAllocations).set(updates).where(eq(proxyAllocations.id,id));await audit({id:user.id,role:user.role},"proxy.update","proxy",id,updates,req);return NextResponse.json({ok:true})}
