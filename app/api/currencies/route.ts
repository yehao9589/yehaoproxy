import{NextResponse}from"next/server";import{asc,eq}from"drizzle-orm";import{getDb}from"../../../db";import{currencies,systemOptions}from"../../../db/schema";export async function GET(){const db=getDb(),items=await db.select().from(currencies).where(eq(currencies.enabled,true)).orderBy(asc(currencies.sortOrder)),[option]=await db.select().from(systemOptions).where(eq(systemOptions.key,"currency_switch_enabled")).limit(1);return NextResponse.json({enabled:option?.value!=="false",items})}
