import{NextResponse}from"next/server";import{and,eq}from"drizzle-orm";import{getCurrentCustomer}from"../../../../../lib/auth";import{audit}from"../../../../../lib/audit";import{getDb}from"../../../../../db";import{inventory,notifications,orders,proxyAllocations}from"../../../../../db/schema";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentCustomer();if(!user)return NextResponse.json({error:"请先登录"},{status:401});const{id}=await params,db=getDb(),[order]=await db.select().from(orders).where(and(eq(orders.id,id),eq(orders.customerEmail,user.email))).limit(1);if(!order)return NextResponse.json({error:"订单不存在"},{status:404});if(order.status!=="paid")return NextResponse.json({error:order.status==="provisioning"?"该额度已进入人工开通流程":"当前额度不能提取"},{status:409});const stock=await db.select().from(inventory).where(and(eq(inventory.product,order.product),eq(inventory.country,order.region),eq(inventory.status,"available"))).limit(order.quantity),now=new Date();if(stock.length===order.quantity){for(const item of stock){await db.insert(proxyAllocations).values({id:crypto.randomUUID(),orderId:id,host:item.host,port:item.port,username:item.username,encryptedPassword:item.encryptedPassword,protocol:item.protocol,expiresAt:new Date(now.getTime()+order.durationDays*86400000),status:"active"});await db.update(inventory).set({status:"allocated",reservedByOrderId:id,updatedAt:now}).where(and(eq(inventory.id,item.id),eq(inventory.status,"available")))}await db.update(orders).set({status:"active",updatedAt:new Date()}).where(eq(orders.id,id));await db.insert(notifications).values({id:crypto.randomUUID(),customerId:user.id,type:"extraction",title:"IP 已提取成功",body:`订单 ${id} 已从库存中心自动发放 ${stock.length} 条 ${order.region} IP。`,link:"/dashboard",read:false,createdAt:now});await audit({id:user.id,role:user.role},"order.auto_extract","order",id,{region:order.region,quantity:stock.length},req);return NextResponse.json({ok:true,status:"active",allocated:stock.length,manualRequired:false,message:`提取成功，${stock.length} 条 IP 已发放到“我的代理”。`})}await db.update(orders).set({status:"provisioning",updatedAt:now}).where(eq(orders.id,id));await db.insert(notifications).values({id:crypto.randomUUID(),customerId:user.id,type:"extraction",title:"等待管理员手动开通",body:`库存中心只有 ${stock.length} 条可用 ${order.region} IP，需要 ${order.quantity} 条。管理员采购入库后将为你开通。`,link:"/dashboard",read:false,createdAt:now});await audit({id:user.id,role:user.role},"order.manual_extraction_required","order",id,{region:order.region,required:order.quantity,available:stock.length},req);return NextResponse.json({ok:true,status:"provisioning",manualRequired:true,available:stock.length,required:order.quantity,message:`库存暂时不足，已通知管理员手动开通（需要 ${order.quantity} 条，当前 ${stock.length} 条）。`})}
