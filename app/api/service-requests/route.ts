import {and,desc,eq} from "drizzle-orm";
import {NextResponse} from "next/server";
import {getDb} from "../../../db";
import {inventory,orders,proxyAllocations,serviceRequests,systemOptions} from "../../../db/schema";
import {audit} from "../../../lib/audit";
import {getCurrentCustomer} from "../../../lib/auth";
const DAY=86400000;
export async function GET(){const user=await getCurrentCustomer();if(!user)return NextResponse.json({error:"请先登录"},{status:401});const items=await getDb().select().from(serviceRequests).where(eq(serviceRequests.customerId,user.id)).orderBy(desc(serviceRequests.createdAt)).limit(100);return NextResponse.json({items})}
export async function POST(req:Request){const user=await getCurrentCustomer();if(!user)return NextResponse.json({error:"请先登录"},{status:401});const body=await req.json().catch(()=>null),type=String(body?.type||""),allocationId=String(body?.allocationId||"");if(!["renew","replace"].includes(type)||!allocationId)return NextResponse.json({error:"申请参数无效"},{status:400});const db=getDb(),[owned]=await db.select({allocation:proxyAllocations,order:orders}).from(proxyAllocations).innerJoin(orders,eq(proxyAllocations.orderId,orders.id)).where(and(eq(proxyAllocations.id,allocationId),eq(orders.customerEmail,user.email),eq(proxyAllocations.status,"active"))).limit(1);if(!owned)return NextResponse.json({error:"代理不存在或不可操作"},{status:404});const[pending]=await db.select().from(serviceRequests).where(and(eq(serviceRequests.allocationId,allocationId),eq(serviceRequests.type,type as "renew"|"replace"),eq(serviceRequests.status,"pending"))).limit(1);if(pending)return NextResponse.json({error:"已有相同申请正在处理"},{status:409});const now=new Date();if(type==="replace"){const expiry=owned.allocation.expiresAt||owned.order.expiresAt;if(!expiry)return NextResponse.json({error:"该代理缺少提取时间，无法申请更换，请联系管理员"},{status:409});const eligibleUntil=new Date(expiry.getTime()-owned.order.durationDays*DAY+3*DAY),reason=String(body?.reason||"").trim().slice(0,500);if(reason.length<5)return NextResponse.json({error:"请填写至少 5 个字的更换原因"},{status:400});const[usedFreeReplacement]=await db.select({id:serviceRequests.id}).from(serviceRequests).where(and(eq(serviceRequests.allocationId,allocationId),eq(serviceRequests.type,"replace"),eq(serviceRequests.status,"completed"))).limit(1);if(now<=eligibleUntil&&!usedFreeReplacement){const id=`SR-${crypto.randomUUID().slice(0,10)}`;await db.insert(serviceRequests).values({id,customerId:user.id,allocationId,type:"replace",durationDays:null,reason:`${reason}（提取后 3 天内免费更换）`,amount:0,status:"pending",createdAt:now,updatedAt:now});await audit({id:user.id,role:user.role},"service.replace.free_create","service_request",id,{allocationId,eligibleUntil},req);return NextResponse.json({id,status:"pending",amount:0,free:true,message:"免费更换申请已提交"},{status:201})}const[option]=await db.select().from(systemOptions).where(eq(systemOptions.key,"ipReplacementPrice")).limit(1),configured=Number(option?.value??5),amount=Number.isFinite(configured)&&configured>=0?Number(configured.toFixed(2)):5,orderId=`RP-${crypto.randomUUID().slice(0,8).toUpperCase()}`;await db.insert(orders).values({id:orderId,customerEmail:user.email,product:"ip-replacement",region:owned.order.region,quantity:1,durationDays:0,amount,currency:owned.order.currency,status:"pending",paymentMethod:"balance",adminNote:`[REPLACE_ALLOCATION]${allocationId}\n[REPLACE_REASON]${reason}`,createdAt:now,updatedAt:now});await audit({id:user.id,role:user.role},"service.replace.order_create","order",orderId,{allocationId,reason,amount},req);return NextResponse.json({id:orderId,orderId,status:"pending",amount,free:false},{status:201})}const durationDays=Number(body?.durationDays);if(![7,30,90].includes(durationDays))return NextResponse.json({error:"续费时长无效"},{status:400});const[stock]=await db.select({salePrice:inventory.salePrice}).from(inventory).where(and(eq(inventory.host,owned.allocation.host),eq(inventory.port,owned.allocation.port))).limit(1),multiplier=durationDays===7?.35:durationDays===30?1:2.55,amount=Number(((stock?.salePrice||0)*multiplier).toFixed(2)),id=`SR-${crypto.randomUUID().slice(0,10)}`;await db.insert(serviceRequests).values({id,customerId:user.id,allocationId,type:"renew",durationDays,reason:null,amount,status:"pending",createdAt:now,updatedAt:now});await audit({id:user.id,role:user.role},"service.renew.create","service_request",id,{allocationId,durationDays},req);return NextResponse.json({id,status:"pending",amount},{status:201})}
