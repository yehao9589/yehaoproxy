import{NextResponse}from"next/server";import{requireAdminApi}from"../../../../lib/admin-auth";import{getDb}from"../../../../db";import{paymentGateways,suppliers}from"../../../../db/schema";
export async function POST(req:Request){if(!await requireAdminApi())return NextResponse.json({error:"无管理员权限"},{status:403});const b=await req.json().catch(()=>null);if(!b||!["payment","supplier"].includes(b.kind)||!b.id)return NextResponse.json({error:"配置参数无效"},{status:400});const now=new Date();if(b.kind==="payment"){if(!["stripe","alipay","wechat","usdt","paypal"].includes(b.type))return NextResponse.json({error:"支付类型无效"},{status:400});await getDb().insert(paymentGateways).values({id:String(b.id),name:String(b.name||b.id),type:b.type,enabled:Boolean(b.enabled),priority:Number(b.priority||100),supportedCurrencies:String(b.currencies||"USD"),secretRef:b.secretRef?String(b.secretRef):null,webhookSecretRef:b.webhookSecretRef?String(b.webhookSecretRef):null,createdAt:now,updatedAt:now}).onConflictDoUpdate({target:paymentGateways.id,set:{name:String(b.name||b.id),enabled:Boolean(b.enabled),priority:Number(b.priority||100),updatedAt:now}})}else await getDb().insert(suppliers).values({id:String(b.id),name:String(b.name||b.id),adapter:String(b.adapter||"generic-rest"),enabled:Boolean(b.enabled),priority:Number(b.priority||100),apiBaseUrl:b.apiBaseUrl?String(b.apiBaseUrl):null,credentialRef:b.credentialRef?String(b.credentialRef):null,createdAt:now,updatedAt:now}).onConflictDoUpdate({target:suppliers.id,set:{name:String(b.name||b.id),enabled:Boolean(b.enabled),priority:Number(b.priority||100),apiBaseUrl:b.apiBaseUrl?String(b.apiBaseUrl):null,credentialRef:b.credentialRef?String(b.credentialRef):null,updatedAt:now}});return NextResponse.json({ok:true})}
