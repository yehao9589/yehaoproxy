import { NextResponse } from "next/server";import { inArray } from "drizzle-orm";import { requireAdminApi } from "../../../../lib/admin-auth";import { getDb } from "../../../../db";import { inventory } from "../../../../db/schema";import { encryptCredential,inventoryFingerprint } from "../../../../lib/inventory-crypto";
export async function POST(req:Request){if(!await requireAdminApi())return NextResponse.json({error:"无管理员权限"},{status:403});const b=await req.json().catch(()=>null),lines=String(b?.lines||"").split(/\r?\n/).map(x=>x.trim()).filter(Boolean);if(!lines.length||lines.length>5000)return NextResponse.json({error:"每次请输入 1–5000 条代理"},{status:400});if(!Number.isFinite(Number(b.salePrice))||Number(b.salePrice)<=0)return NextResponse.json({error:"销售价格无效"},{status:400});const parsed=[] as Array<{host:string,port:number,username:string|null,password:string|null,fingerprint:string}>;const seen=new Set<string>();for(let i=0;i<lines.length;i++){const [host,port,user,password]=lines[i].split(":");if(!host||!Number.isInteger(Number(port))||Number(port)<1||Number(port)>65535)return NextResponse.json({error:`第 ${i+1} 行格式错误`},{status:400});const fp=await inventoryFingerprint(host,Number(port),user||null);if(seen.has(fp))return NextResponse.json({error:`第 ${i+1} 行在本次导入中重复`},{status:409});seen.add(fp);parsed.push({host,port:Number(port),username:user||null,password:password||null,fingerprint:fp})}const db=getDb(),existing=await db.select({fingerprint:inventory.fingerprint}).from(inventory).where(inArray(inventory.fingerprint,[...seen]));if(existing.length)return NextResponse.json({error:`发现 ${existing.length} 条库存已存在`,duplicates:existing.length},{status:409});const now=new Date(),values=[];for(const x of parsed)values.push({id:`INV-${crypto.randomUUID()}`,source:"manual" as const,product:String(b.product||"static-isp"),country:String(b.country||"US").toUpperCase(),city:b.city?String(b.city):null,host:x.host,port:x.port,username:x.username,encryptedPassword:await encryptCredential(x.password||""),fingerprint:x.fingerprint,protocol:String(b.protocol||"HTTPS"),cost:b.cost?Number(b.cost):null,salePrice:Number(b.salePrice),status:"available" as const,createdAt:now,updatedAt:now});await db.insert(inventory).values(values);return NextResponse.json({ok:true,inserted:values.length,duplicates:0},{status:201})}
