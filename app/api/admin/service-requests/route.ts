import { NextResponse } from "next/server";import { desc,eq } from "drizzle-orm";import { requireAdminApi } from "../../../../lib/admin-auth";import { getDb } from "../../../../db";import { customers,serviceRequests } from "../../../../db/schema";
export async function GET(req:Request){if(!await requireAdminApi())return NextResponse.json({error:"无管理员权限"},{status:403});const status=new URL(req.url).searchParams.get("status"),db=getDb(),[rows,customerRows]=await Promise.all([db.select().from(serviceRequests).where(status?eq(serviceRequests.status,status as "pending"|"approved"|"completed"|"rejected"|"cancelled"):undefined).orderBy(desc(serviceRequests.createdAt)).limit(200),db.select({id:customers.id,name:customers.name,email:customers.email}).from(customers).where(eq(customers.role,"customer"))]),customerMap=new Map(customerRows.map(customer=>[customer.id,customer])),items=rows.map(item=>({...item,customerName:customerMap.get(item.customerId)?.name||"未设置名称",customerEmail:customerMap.get(item.customerId)?.email||null}));return NextResponse.json({items})}
