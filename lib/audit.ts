import { getDb } from "../db";import { auditLogs } from "../db/schema";export async function audit(actor:{id:string;role:string},action:string,resourceType:string,resourceId:string|null,detail:unknown,request:Request){await getDb().insert(auditLogs).values({id:crypto.randomUUID(),actorId:actor.id,actorRole:actor.role,action,resourceType,resourceId,detail:JSON.stringify(detail??{}),ipAddress:request.headers.get("cf-connecting-ip"),createdAt:new Date()})}
