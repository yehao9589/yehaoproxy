# YehaoProxy v1.0.1 发布核对清单

本清单用于正式发布前的最后验收。自动化检查通过并不代替宝塔生产环境中的支付、邮件和备份恢复演练。

## 代码与构建

- [x] `pnpm install --frozen-lockfile` 可复现安装
- [x] ESLint 无阻断错误
- [x] TypeScript 类型检查通过
- [x] Vinext 生产构建通过
- [x] 20 项关键业务回归测试全部通过
- [x] Docker 镜像、更新中心和发布清单固定为 `v1.0.1`
- [x] GitHub 镜像发布工作流在构建前执行完整质量门禁
- [x] 宝塔单容器启动时自动补齐历史 MySQL 字段

## 宝塔预发布验证

- [ ] 使用 `docker-compose.single.yml` 部署候选镜像
- [ ] 确认宝塔 MySQL 仅监听本机，应用数据库地址使用 `127.0.0.1`
- [ ] `GET /api/health` 返回 HTTP 200，且 `database: true`
- [ ] 旧数据库直接升级后可以登录，客户、商品、订单、服务和日志页面可正常读取
- [ ] 新建客户并完成一次新购、交付、续费、退款和售后流程
- [ ] 使用优惠券后，支付宝请求金额等于实际应付金额
- [ ] 支付宝异步通知验签成功，订单状态和交易流水只更新一次
- [ ] 支付宝原路退款金额、币种和原支付流水一致
- [ ] 使用余额和信用额分别完成支付，账单与流水的支付方式显示正确
- [ ] SMTP 测试邮件、新购邮件、续费邮件和库存不足管理员邮件均可收到
- [ ] XPanel 或外部供应商连接、开通和续费流程可用（如正式环境启用）

## 数据与恢复

- [ ] 升级前导出一次完整备份并下载到服务器外
- [ ] 在隔离环境导入该备份，核对客户、订单、资源、流水和站务配置数量
- [ ] 确认 `data`、`uploads`、`backups` 使用持久化目录，不随容器重建丢失
- [ ] 验证旧镜像与数据库备份可以回滚

## 正式发布

- [ ] 配置正式域名和 HTTPS，并将 `PUBLIC_APP_URL` 改为正式 HTTPS 地址
- [ ] 确认邮件、支付宝回调和站点 Logo 使用正式域名
- [ ] 完成人工验收后创建 `v1.0.0` Git 标签
- [ ] 确认 `ghcr.io/yehao9589/yehaoproxy:v1.0.0` 构建成功
- [ ] 生产环境只使用固定版本标签，不使用 `latest` 或 `pre-release`
- [ ] 发布后再次检查健康接口、登录、支付回调和后台定时任务

## 已知依赖风险

当前依赖审计仍会报告 Vinext 间接依赖 `image-size@2.0.2` 的安全告警，上游尚未发布包含修复版本的稳定依赖链。站点 Logo 上传接口已增加 PNG、JPEG、WebP 文件签名校验，降低动态上传触发风险。后续 Vinext 发布兼容修复版本后应立即升级并重新运行依赖审计。
