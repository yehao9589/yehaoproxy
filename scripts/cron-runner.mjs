const url=process.env.CRON_URL||"http://yehaoproxy:3000/api/cron/reminders",secret=process.env.CRON_SECRET||"",interval=Math.max(30000,Number(process.env.CRON_INTERVAL_MS||60000));
async function run(){try{const response=await fetch(url,{method:"POST",headers:{authorization:`Bearer ${secret}`}});if(!response.ok)console.error(`YehaoProxy scheduled task failed: ${response.status}`,await response.text());else console.log("YehaoProxy scheduled task completed",new Date().toISOString())}catch(error){console.error("YehaoProxy scheduler waiting for application",error instanceof Error?error.message:error)}}
setTimeout(run,10000);setInterval(run,interval);console.log(`YehaoProxy scheduler started, interval ${interval}ms`);
